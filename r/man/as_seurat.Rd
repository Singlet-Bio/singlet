% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat.R
\name{as_seurat}
\alias{as_seurat}
\title{Convert a singlet pipeline directory to a Seurat object.}
\usage{
as_seurat(path, primary_assay = "spliced", project = NULL)
}
\arguments{
\item{path}{Directory containing the pipeline's ``.1pz`` + TSV files.}

\item{primary_assay}{Matrix name for the default ``RNA`` assay. Tried
in order: user value, ``"spliced"``, ``"gene_counts"``, ``"gene_counts_em"``.}

\item{project}{Project name attached to the Seurat object. Defaults to
the ``gsm_id`` from the embedded pipeline metadata.}
}
\value{
A \code{Seurat} object.
}
\description{
Builds a \code{\link[Seurat:Seurat]{Seurat}} object from a singlet
pipeline output directory. The primary per-gene matrix becomes the
default ``RNA`` assay; the velocity trio
(``spliced`` / ``unspliced`` / ``ambiguous``) become additional assays
when present, named matching the scvelo convention so velocity
analyses work without extra renaming.
}
\details{
Per-cell TSV sidecars are loaded into ``obj@meta.data``. The embedded
GEO metadata goes into ``obj@misc$singlet``.
}
\examples{
\dontrun{
obj <- as_seurat("quant/scrna/GSE174/GSE174399/GSM5293863")
obj
obj@misc$singlet[["gsm_id"]]
}

}
