% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load.R
\name{find_load}
\alias{find_load}
\title{Search and load in one step}
\usage{
find_load(
  query,
  as = c("sce", "seurat"),
  level = c("gse", "gsm"),
  limit = 10L,
  cache_dir = NULL
)
}
\arguments{
\item{query}{Free-text search string (see \code{\link{find}}).}

\item{as}{Output class, \code{"sce"} (default) or \code{"seurat"}.}

\item{level}{Accession granularity, \code{"gse"} (default here, since
whole Series combine cleanly) or \code{"gsm"}.}

\item{limit}{Maximum number of accessions to load. Default \code{10}.}

\item{cache_dir}{Bundle cache directory (see \code{\link{load}}).}
}
\value{
A combined \code{SingleCellExperiment} or \code{Seurat} object, or
  \code{NULL} (invisibly) if the search returns no matches.
}
\description{
Convenience wrapper that runs \code{\link{find}} and immediately passes
the matching accessions to \code{\link{load}}.
}
\examples{
\dontrun{
sce <- find_load("microglia from Alzheimer's brain", limit = 5)
}

}
\seealso{
\code{\link{find}}, \code{\link{load}}
}
