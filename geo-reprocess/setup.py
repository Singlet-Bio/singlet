"""
Setup script for sc-geo package.

This file exists to support editable installs:
    pip install -e .

Build configuration is in pyproject.toml (PEP 517/518).
"""
from setuptools import setup

setup()
