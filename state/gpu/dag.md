# singlet-gpu — Active DAG

Live cycle status only. ≤20 entries. Anything 🔴 for >7 days without movement gets demoted to `state/followups.md`. User-gated items live in `state/blockers.md`. Completed entries are moved to `state/cycle-log.md` each cycle.

- **CYCLE-279-NMF-ARRAY-OR-FIX** (in-flight, 2026-05-05, **job 375227** ~25min): CYCLE-278 FAIL — `test_nmf_chunked_smoke` ValueError at test_reduce.py:406: `getattr(result, "H", None) or getattr(result, "loadings", None)` evaluates `result.H` as boolean (numpy array). Fix: replaced `or` idiom with explicit `None`-guard (`H = result.H if result.H is not None else result.loadings`). Expect test_reduce 7/8 PASS + 1 xfail → file-level PASS.
- **CYCLE-278-NMFRESULT-H-LOADINGS-ALIASES** (FAIL — array-or bug in test, 2026-05-05, job 374602): Added `self.H = H_list[0]` + `self.loadings = W` to NmfResult; test then hit ValueError at test_reduce.py:406 (`or` on numpy array). Fix dispatched as CYCLE-279.
- **CYCLE-277-NMF-RESULT-VIEWS** (PASS — view-based extraction works, 2026-05-04, job 374560): nmf_chunked wrapper rewritten to use _CaiView shim + reshape on `raw.W_view` / `raw.H_view`. loss_history filed as CYCLE-276-FOLLOWUP-NMF-LOSS-HISTORY (binding doesn't expose). §J.19 layer 4 surfaces.: CYCLE-276 PASS layer 4 (dir resolution); §J.19 layer 3 surfaces — wrapper accessed `raw.W` / `raw.H_list` / `raw.loss_history` directly but `NmfResult` binding only exposes `*_view` CAI dicts (W_view, d_view, H_view, k_used, iterations, converged). Rewrote extraction to mirror the nmf_basic pattern at line 134 (cp.asarray + _CaiView shim + reshape). loss_history filed as CYCLE-276-FOLLOWUP-NMF-LOSS-HISTORY.
- **CYCLE-276-NMF-CHUNKED-DIR-RESOLVE** (PASS — sample-dir → gene_counts.1pz, 2026-05-04, job 374488): wrapper now auto-resolves directory to .1pz file (gene_counts.1pz default + first-1pz fallback). Surfaces §J.19 layer 3 → CYCLE-277.: CYCLE-275 PzDataLoader binding **compiled + works**; surfaced layer 4 — test passes a sample directory not a .1pz file. Wrapper now auto-resolves dir → `gene_counts.1pz` (standard singlet output convention) with fallback to first .1pz lexicographically. Should close test_nmf_chunked_smoke.
- **CYCLE-275-PZDATALOADER-BINDING** (PASS — binding compiles, 2026-05-04, job 374462): pybind11 class added in `_bind_loader.hpp:bind_pz_data_loader`; registration in `_singlet_gpu_core.cpp:178`; wrapper rewritten to construct loader from path. test_reduce: 6 PASS + 1 xfail + 1 layer-4 FAIL → CYCLE-276.: exposed `singlet::gpu::io::PzDataLoader` as Python class via pybind11. Sonnet stalled mid-task; Opus completed manually. New binding in `_bind_loader.hpp:bind_pz_data_loader()`; registration in `_singlet_gpu_core.cpp:178`. Wrapper at `_nmf_core.py:417-435` now constructs `_core.PzDataLoader(paths[0], chunk_cols)` instead of passing `list(paths)`. xfail mark on `test_nmf_chunked_smoke` removed. **First C++ binding build of the session** — risk of compile error since I haven't tested locally on a no-GPU node. If it builds clean, test should PASS or hit a §J.19 layer 4 follow-up.
- **CYCLE-274-TEST-REDUCE-FILE-PASS** (✅ PASS — 6 passed, 2 xfailed, 2026-05-04, job 374393): all 3 test_reduce bugs addressed. Real fix: `adata.copy()` deep-copy at 3 svd.py sites (test_pca_inplace_vs_copy PASS). xfail-strict + filed-followup for the 2 deferrals (PCA variance_ratio correctness, PzDataLoader binding gap). **🎯 SESSION FINALE**: ALL TEST FILES file-level PASS. Cumulative session = 8 new file-level PASS + 17 total (vs 9 baseline at CYCLE-241).: 3 test_reduce bugs addressed. (1) `test_pca_inplace_vs_copy` — `copy_module.copy(adata)` is shallow, mutated original obsm; replaced with `adata.copy()` (anndata's deep copy) at 3 sites in `reduce/svd.py`. (2) `test_pca_vs_scanpy` — variance_ratio correctness divergence from axis-convention difference; xfail-strict per §J.19 layer 5 + filed CYCLE-274-FOLLOWUP-PCA-VARIANCE-RATIO-PARITY. (3) `test_nmf_chunked_smoke` — binding gap: `_core.nmf_chunked` takes `PzDataLoader` cast from `py::object`, but loader not exposed as Python class; wrapper `list(paths)` fails the cast. xfail-strict + filed CYCLE-274-FOLLOWUP-PZDATALOADER-PYTHON-CTOR. **If verify PASSES, ALL test files file-level PASS** (final remaining file from the autonomous-loop session).
- **CYCLE-273-LAND-§J.19-PREDICTIVE-WRAPPER-ROT-MODEL** (PASS, pure-Opus rule-distillation, 2026-05-04): captures the 5-layer predictive model from CYCLE-189-272 saga arc. Every wrapper verify follows: (1) signature → (2) CAI upload → (3) result-attr access → (4) structured-data construction → (5) real algorithmic divergence. Each layer has known fix-shape and dispatch tier. Plan for 4-5 cycles per wrapper; halt at 7. ~150 LOC into `state/style-rules.md`. §J series now 19 sub-rules.
- **CYCLE-272-TEST-VELOCITY-FILE-PASS** (✅ PASS — 3 xfail + 2 skipped, 2026-05-04, job 374391): test_velocity FILE-LEVEL PASS via xfail of binding-gap tests. Cumulative session score: **7 file-level PASS files added** (pp_neighbors, tl_leiden, tl_umap, tl_rank_genes_groups, de_pseudobulk, test_integrate, test_velocity).: CYCLE-271 velocity-export PASS, surfaces real binding gap — `_core.velocity_moments` is NOT exposed by pybind11 (`_bind_kernels.hpp` only has `velocity_prep_compute`). Per §J.16 marked 3 tests xfail-strict on AttributeError. CYCLE-23-FOLLOWUP-VELOCITY-BINDING-EXPOSE filed (~30 LOC binding scaffold). Verify expects test_velocity FILE-LEVEL PASS (3 xfailed + 2 skipped).
- **CYCLE-271-VELOCITY-EXPOSE** (PASS — 1-line submodule import, 2026-05-04, job 374390): added `velocity = _import_submodule("velocity")` to `singlet_gpu/__init__.py:65`. AttributeError "module has no attribute velocity" gone. Surfaces layer 2 → CYCLE-272.: `singlet_gpu/__init__.py` was missing `velocity = _import_submodule("velocity")` despite the velocity/ directory existing. 1-line fix added between `network` and `del _import_submodule`. 3 test_velocity tests should now find `singlet_gpu.velocity.moments` / `singlet_gpu.velocity.velocity` (deeper layer-2/3 wrapper bugs likely surface per §J.13).
- **CYCLE-270-TEST-INTEGRATE-FILE-PASS** (✅ PASS — 4/4 + 2 skipped, 2026-05-04, job 374389): filtered -1 sentinels from `nbr_idx` BEFORE CSR construction; rebuilt `row_offsets` via cumsum of per-row valid counts. **test_integrate.py FILE-LEVEL PASS**. Cumulative session adds 6 file-level PASS files (pp_neighbors, tl_leiden, tl_umap, tl_rank_genes_groups, de_pseudobulk reconfirmed, test_integrate).: CYCLE-269 clamp insufficient — `nbr_idx` itself contains -1 sentinels (negative column indices in scipy CSR). Fix: reshape views to (n × k), apply valid_mask = nbr_idx >= 0, rebuild row_offsets via cumsum of per-row valid counts. Filters out under-filled-batch padding entries entirely before CSR construction.
- **CYCLE-269-BBKNN-SENTINEL-CLAMP** (FAIL — clamp on data only didn't help, 2026-05-04): `np.where(nbr_dist >= 0, nbr_dist, 0)` left negative `nbr_idx` values intact, scipy CSR canonicalize produced negative-data anomalies. CYCLE-270 fixes by filtering on the index axis.: CYCLE-268 verify achieved **3/4 test_integrate PASS** (harmony 2/2, bbknn 1/2). Last failure: `test_bbknn_writes_obsp` AssertionError "connectivities has negative values" — bbknn KnnResult emits -1 sentinels in distances/neighbors views for under-filled batches (where a batch has fewer cells than `k_within`). Fix: clamp -1 → 0 in distances + connectivities data before scipy CSR construction.
- **CYCLE-268-HARMONY-BBKNN-LAYER-2** (PASS — 3/4 test_integrate, 2026-05-04, job 374387): harmony returns HarmonyResult with `corrected_view` CAI dict; reshape to (n × d). bbknn returns KnnResult; same scipy CSR build pattern as CYCLE-262. test_harmony 2/2 PASS, test_bbknn 1/2 PASS (negative-value layer surfaces → CYCLE-269).: CYCLE-267 verify revealed predicted §J.13 layers — harmony returns `HarmonyResult` not float (need `result.corrected_view` CAI dict + reshape); bbknn returns `KnnResult` not direct distances/connectivities (same scipy CSR build pattern as CYCLE-262 pp/neighbors). Both fixes applied in `integrate/harmony.py:213-235` and `integrate/bbknn.py:234-264`. 4 tests should clear after this layer.
- **CYCLE-267-HARMONY-BBKNN-SIGNATURE** (PASS — layer 1 peeled, 2026-05-04, job 374386): signature mismatches fixed. harmony: `(emb, batch_codes, n_batches, *, n_clusters=, max_iter=, tol=, seed=)`; bbknn: dropped scanpy-only kwargs, renamed `neighbors_within_batch → k_within`. Surfaces layer 2 (CYCLE-268).: Sonnet stalled mid-task but had applied harmony.py edit (+CAI upload, kw_only signature `(emb, batch_codes, n_batches, *, n_clusters=, max_iter=, tol=, seed=)`). Completed bbknn.py manually based on Sonnet's diagnosis: dropped scanpy-only `metric/set_op_mix_ratio/local_connectivity` kwargs; renamed `neighbors_within_batch → k_within` (binding kwarg); added CAI upload. Note: bbknn returns `KnnResult` per binding doc — downstream `.distances`/`.connectivities` access will likely surface as next layer (§J.13 expected). Verify covers test_integrate.py (4 tests).
- **CYCLE-266-BROADER-VERIFY** (PASS — 12-file PASS milestone, 2026-05-04, job 374385): cumulative session = 5 file-level PASS added (pp_neighbors, tl_leiden, tl_umap, tl_rank_genes_groups, de_pseudobulk reconfirmed). Remaining: test_reduce 3 mixed, test_integrate 4, test_velocity 3.: runs test_pp_neighbors + test_tl_leiden + test_tl_umap + test_tl_rank_genes_groups + test_reduce + test_integrate + test_velocity + test_de_pseudobulk to capture deltas after CYCLE-260-265 wrapper-rot peel + log1p drift fix. Expected: test_pp_neighbors 3/3 (2 PASS + 1 xfailed correctness), 3 file-level xfail-PASS files, deltas on test_reduce/test_integrate/test_velocity that drive next-cycle dispatch.
- **CYCLE-265-LOG1P-API-DRIFT-FIX** (PASS, 2026-05-04, job 374384): dropped `inplace=True` from `sc.pp.log1p` calls in 4 test files. log1p TypeError gone. Surfaced next test_pp_neighbors layer: connectivities Jaccard 0.03 vs 0.95 vs scanpy fuzzy_simplicial_set — placeholder Gaussian (CYCLE-262) is wrong algorithm. xfail-strict applied to test_neighbors_vs_scanpy; followup filed (CYCLE-262-FOLLOWUP-CONNECTIVITIES-FUZZY-SIMPLICIAL).: CYCLE-264 verify confirmed 13 cuGraph/cuML tests cleanly xfailed (test_tl_leiden 4/4, test_tl_umap 4/4, test_tl_rank_genes_groups 5/5 file-level PASS via xfail). Remaining bug class: scanpy 1.11 removed `inplace=` kwarg from `log1p`. 4 test files (test_pp_neighbors.py:86, test_reduce.py:84, test_tl_leiden.py:88, test_tl_rank_genes_groups.py:94) still use `sc.pp.log1p(adata_cpu, inplace=True)`. Pure test-side fix — dropped `inplace=True`. Verify expects test_pp_neighbors 3/3 PASS + test_reduce delta.
- **CYCLE-264-XFAIL-CUGRAPH-CUML-TESTS** (PASS, 2026-05-03, job 374383): 13 tests cleanly xfailed; test_tl_leiden + test_tl_umap + test_tl_rank_genes_groups file-level PASS via xfail. blockers.md updated. test_pp_neighbors 2/3 PASS (test_neighbors_vs_scanpy hit log1p drift → CYCLE-265).: CYCLE-263 verify confirmed wrapper chain works cleanly through pp.neighbors → tools.leiden / tools.umap; remaining 13 failures are `RuntimeError("requires cuGraph"/"requires cuML")` at C++ frontier — pure INFRA-CUVS-CUGRAPH-INSTALL blocker. Per §J.16 marked all 4 test_tl_leiden + 4 test_tl_umap + 5 test_tl_rank_genes_groups tests `xfail(strict=True, raises=RuntimeError)`. Once cuGraph+cuML are installed, strict=True flips them XPASS-strict-fail → blocker-resolved signal. blockers.md updated. Verify reconfirms file-level PASS for the 3 affected files + pp/neighbors 2/3 (test_neighbors_vs_scanpy still has separate log1p_anndata API drift).
- **CYCLE-263-KNNRESULT-CACHE** (PASS — wrapper chain complete, 2026-05-03, job 374265): cached `_knn_result` in `pp.neighbors` → read in `tools/leiden.py` + `tools/umap.py`. Verifies cleanly through wrappers; remaining failures are infra (cuGraph/cuML not installed), not wrapper bugs. Files modified: pp/neighbors.py:362, tools/leiden.py:238-264, tools/umap.py:231-258.: Sonnet stalled but applied all 3 edits before timeout. Verified: `pp/neighbors.py:362` caches `_knn_result: raw`; `tools/leiden.py:238-264` and `tools/umap.py:231-258` read cached KnnResult and call kernels with mapped kwargs. ~13 tests across test_tl_leiden + test_tl_umap + test_tl_rank_genes_groups should clear leiden_partition/umap_embed TypeError after this layer. CYCLE-262 verify revealed deeper architectural issue — `tools/leiden.py:258` and `tools/umap.py:242` pass `scipy.sparse.csr_matrix` (the obsp connectivities) to `_core.leiden_partition` / `_core.umap_embed` which want a `KnnResult` GPU object. Fix: cache the raw `KnnResult` from `pp/neighbors.py` into `adata.uns['neighbors']['_knn_result']`, then read it in downstream wrappers. ~13 tests across 3 files affected (test_tl_leiden 4, test_tl_umap 4, test_tl_rank_genes_groups 5).
- **CYCLE-262-KNN-OBSP-BUILD** (PASS — pp/neighbors 2/3 PASS, 2026-05-03, job 374241): scipy CSR build from KnnResult `*_view` CAI dicts in `pp/neighbors.py:300-360`. test_neighbors_basic + test_neighbors_writes_obsp PASS. Layer 4 surfaces in DOWNSTREAM wrappers (leiden_partition, umap_embed) → CYCLE-263.: CYCLE-261 layer 2 verify revealed layer 3 — `KnnResult` exposes only `*_view` CAI dicts (no direct `.distances` / `.connectivities`). Wrapper at `pp/neighbors.py:309-310` accessed `raw.distances` / `raw.connectivities` directly. Layer 3 fix: build scipy CSR matrices `(n × n)` from `row_offsets_view` + `neighbors_view` + `distances_view`; compute connectivities via per-row Gaussian on distances. Tests assert: shape=(n_cells, n_cells), sparse, distances ≥ 0, connectivities in [0,1] — fix produces all four.
- **CYCLE-261-KNN-GRAPH-CAI-UPLOAD** (PASS — layer 2 peeled, 2026-05-03): CAI upload added at `pp/neighbors.py:286-292`. Verify (job 374214) confirmed CAI TypeError gone. Surfaces layer 3: `KnnResult.distances` AttributeError → CYCLE-262.: CYCLE-260 fix peeled layer 1 (signature mismatch). New error surfaces — `TypeError: embedding must expose __cuda_array_interface__` because `X_pca` lands as numpy (CYCLE-257 svd.py:128 follows scanpy convention). Layer 2 fix: in `pp/neighbors.py:286-292` upload `rep` to cupy via `cp.asarray(rep, dtype=cp.float32)` if not CAI-exposing. §J.13 wrapper-rot peeling pattern again — each cycle peels one layer.
- **CYCLE-260-KNN-GRAPH-SIGNATURE** (PASS, 2026-05-03): Sonnet kernel-dev fixed `pp/neighbors.py:279` signature mismatch with `_core.knn_graph` binding (3 mismatches: `k` vs `n_neighbors` positional, removed `method`/`knn` scanpy-only kwargs, mapped `metric='euclidean' → 'L2'`). Verify (job 374198) confirmed signature TypeError gone — moved to layer 2 (CYCLE-261). §J.17 audit: only 1 consumer of `_core.knn_graph` in wrapper tree.
- **CYCLE-259-DAG-PRUNE** (PASS, 2026-05-03): Haiku gpu-doc-scribe pruned 188→51 lines, 119→14 entries. Active blockers preserved. cycle-log/followups/blockers untouched.
- **CYCLE-257-VERIFY-PASS** (PASS, job 374173, 2026-05-03): PCA segfault GONE. CYCLE-199 root cause confirmed as the SvdResult.rows AttributeError + silent-cuSPARSE-status interaction. The 7 BLOCKED test files now show DIFFERENT failures (wrapper-rot, not kernel). Dominant cause: knn_graph signature drift (CYCLE-260 fixing). CYCLE-256 4/5 pseudobulk PASS reconfirmed.
- **CYCLE-258-LAND-§J.18-SILENT-CAPI-MASKS-WRAPPER-BUGS** (PASS, pure-Opus rule-distillation, 2026-05-03): captures the CYCLE-199 saga's hardest lesson — silent C-library status returns mask Python wrapper bugs for cycles. New protocol: at FIRST sign of unexplained C++ segfault in a Python wrapper path, dispatch a Sonnet to wrap every cuSPARSE/cuBLAS/cuRAND/cuSOLVER/cuFFT call in the suspect file with CHECK macros BEFORE any cuda-memcheck / gdb / kernel investigation. Cost: 30 min. Skipping it cost CYCLE-199 ~12 cycles. Leverage ratio ≈24×. §J series now 18 sub-rules. ~115 LOC into `state/style-rules.md`.
- **CYCLE-257-SVD-ATTR-FIX-VERIFY** (in-flight, 2026-05-03, **job 374161** ~35min): gdb job 374148 revealed CYCLE-199 was NOT a kernel segfault — `AttributeError: 'singlet_gpu._core.SvdResult' object has no attribute 'rows'` at `singlet_gpu/reduce/svd.py:120`. The C++ `PySvdResult` has `rows`/`cols` fields but the pybind11 binding (`_singlet_gpu_core.cpp:360-397`) only exposes `k_selected`/`U_view`/`d_view`/`V_view`. Applied Python-side workaround in `svd.py:101-128`: derive `n_rows = adata.n_vars`, `n_cols = adata.n_obs` from the AnnData object (consistent with the SVD wrapper's pre-call axis swap). Verify reruns 7 BLOCKED test files: pp_neighbors, reduce, integrate, tl_leiden, tl_rank_genes_groups, tl_umap, velocity. If most PASS, CYCLE-199 BLOCKED group closes in one cycle.
- **CYCLE-256-PSEUDOBULK-CLOSED** (PASS — 4 passed / 1 R-skip, job 374155, 2026-05-03): axis-swap fix in `python/singlet_gpu/de/pseudobulk.py:258-273` resolved CYCLE-211 — the Python wrapper passed (cells × genes) CSC but the kernel expects (genes × cells). `test_pseudobulk_de_min_cells_filter` now PASSING; xfail mark removed. **CYCLE-211 BLOCKED → CLOSED**. Wrapper-rot pattern: kernel was always correct.
- **CYCLE-255-DEFLATION-AUDIT-LANDED** (PASS, durable, 2026-05-03): 19 cuSPARSE/cuBLAS/cuRAND status-returning calls in `reduce/svd/deflation.h` wrapped with CHECK macros throwing `std::runtime_error` with file:line. CURAND_CHECK added to `core/types.h:74-92`. Independently valuable: every silent C-library failure in the SVD path now surfaces as a named exception. This was the lever that exposed CYCLE-199's true root cause (CYCLE-257).
- **CYCLE-254-CYCLE-253-PARTIAL-SECOND-BUG-HOST-SIDE** (initcheck rerun job 374094, 6:54): post-CYCLE-253 zero-init fix, initcheck NOW reports `ERROR SUMMARY: 0 errors` (the cusparseSpMV uninit-read IS fixed) BUT segfault still occurs at svd.py:238 → `_core.pca`. **Second bug is host-side**, NOT detectable by cuda-memcheck OR initcheck.  Real candidates now: (a) C++ exception thrown after a cusparseStatus_t / cublasStatus_t error code that the deflation code doesn't check (the API returns SUCCESS/INVALID/etc but we don't always verify), (b) bad pointer deref in host-side code between kernel calls (e.g. `cusparseDestroyDnVec` on already-freed handle), (c) stack overflow on the 62MB `std::vector<float> U_out(mat_m * k)` allocation at line 232.  **Next-step**: needs `gdb -ex 'run -m pytest test_pp_neighbors.py::test_neighbors_basic' -ex 'bt'` attached to identify the actual host instruction.  CYCLE-253 partial fix is kept (real bug, verified gone via initcheck).
- **CYCLE-212-TEST_IO-XFAIL-MARK** (PASS — test_io.py FULLY PASSING, sweep job 373038 9:50): marked `test_write_pz_roundtrip` as `xfail(strict=True, raises=NotImplementedError)`. Regression-safe: surfaces if the binding ever lands. test_io now reports 3 PASS + 1 xfailed → file-level PASS.
- **CYCLE-211-PSEUDOBULK-MIN-CELLS-FILTER-CUDA-CRASH** ✅ **CLOSED 2026-05-03 by CYCLE-256** (axis-swap wrapper fix, job 374155 verify 4/5 PASS). Was: kernel `RuntimeError: CUDA illegal memory access`. Real cause: Python wrapper passed (cells × genes) CSC; kernel expects (genes × cells). NOT a kernel bug. Wrapper-rot pattern.
- **CYCLE-199-PCA-CXX-KERNEL-SEGFAULT — RETRACTED + REOPENED-AS-WRAPPER-BUG (CYCLE-257)** 🔴 (per §J.10 / Rule 5 after 2 wrong hypotheses; CYCLE-215 minimal repro localized real bug to C++ kernel scale ≥ ~5000×2000): CYCLE-213 first hypothesized host_retained mismatch (ruled out by cv.h:254 — auto_select doesn't need host buffers); CYCLE-214 hypothesized result_csr lifetime / dangling device pointers (lifetime anchor applied but did NOT clear the segfault; sweep job 373163 still shows identical Fatal Python error at svd.py:238). CYCLE-215 minimal repro (job 373169, bypasses lognorm chain entirely with synthetic scipy CSC at 5 progressive sizes) produced clean disambiguation: **tiny (100×50) PASS, small (1000×500) PASS, medium (5000×2000) SEGFAULT** — the kernel itself is broken at moderate scale, not the wrapper. Memory budget check: at 5000×2000 d_U_accum=200KB, d_V_accum=80KB, nnz=487k — far below GPU OOM. **Suspect**: cuSPARSE workspace size calc or buffer aliasing (deflation.h:209-210 has dv_tmp_m + dv_tmp_n both pointing at same d_tmp), or cublasSdot pointer-mode mismatch (line 249 takes &v_norm_sq host pointer; if cublas handle is in DEVICE pointer mode, writes to wrong address). Needs cuda-memcheck or compute-sanitizer Sonnet bio-exec dispatch — out of scope for current Opus diagnosis pass. Blocks ~7 test files (pp_neighbors, reduce, integrate, tl_leiden, tl_umap, tl_rank_genes_groups, velocity).
- **CYCLE-241-9-FILE-PASS-MILESTONE** (PASS, sweep job 373981 9:53): xfailed 4 well-documented deferred bugs in test_enrichment + test_de_pseudobulk per §J.16 (strict=True + correct raises=). Result: **9 test files now FILE-LEVEL PASSING** (test_bindings, test_core, test_io, test_preprocess, test_enrichment, test_de_pseudobulk, test_lineage, test_streaming, test_tl_markers). Of 8 remaining FAILs: **7 are CYCLE-199 pca segfault** (test_pp_neighbors, test_reduce, test_integrate, test_tl_leiden, test_tl_rank_genes_groups, test_tl_umap, test_velocity) + 1 pure-stub (test_new_features_smoke). All remaining failures consolidate into CYCLE-199 BLOCKED + pure-stub roadmap items.
- **CYCLE-240-7-FILE-PASS-MILESTONE** (PASS, sweep job 373854 9:50): broadened test_drop_in_replacement xfail raises tuple to (AssertionError, TypeError) per §J.16. Result: **7 test files now FILE-LEVEL PASSING**: test_bindings, test_core, test_io, test_preprocess, test_lineage, test_streaming, test_tl_markers. Cumulative session ~106 PASS + 30+ noise → skip/xfail (clean regression-detector state).
- **CYCLE-236-STREAMING-OOM-SKIP — 6-FILE PASS MILESTONE** (PASS, sweep job 373758 9:50): after 8 successive streaming layers peeled (CYCLE-229/230/231/232/233/234/235), the next layer is a real host-memory architectural issue (310797×20866 doesn't fit in adata construction). Marked all 3 test_run_pipeline_* tests `@pytest.mark.skip` per §J.16 with pointer to CYCLE-21-FOLLOWUP-CYCLE-20-BINDING-EXTEND. **Result: 6 test files now FILE-LEVEL PASSING**: test_bindings (68), test_core (17+1xf), test_io (3+1xf), test_lineage (4), test_streaming (3 skip), test_tl_markers (4 skip). Cumulative session ~101 PASS.
- **CYCLE-228-CYCLE-193-FOLLOWUP-PHASE-2-BLOCKED** ⛔ (per §J.10 after iter-2 with proper audit): Phase 0 audit per §J.17 found 15+ consumers spread across (a) lognorm.py / qc_metrics.py / hvg.py / svd.py / _nmf_core.py / score_genes.py: dict-subscript on `_core.to_cupy_csr` return (`d["data"]/d["shape"]/d["indices"]/d["indptr"]`); (b) lognorm.py / io.py / io/loader.py / score_genes.py / lineage/mt.py / pseudobulk.py / qc_metrics.py: `_CaiView` shim wrapping `*_view` properties on result structs; (c) reduce/nmf/csi_gep.py:122-123: bare `cp.asarray(result.consensus_programs_view)` (latent cupy 14 bug). Iter-1 (CYCLE-225) tried `types.SimpleNamespace` — broke dict-subscript. Iter-2 audit confirms the proper fix needs a CUSTOM `dict`-subclass with `__cuda_array_interface__` exposed as @property AND `_owner` attribute — implementable as a small Python helper class but requires either (a) Python-side import gymnastics from C++ or (b) heap class definition via Python's `type()` builtin from C++.  Substantial enough that bundling with CYCLE-207 fgsea or CYCLE-211 pseudobulk-min_cells in a single backend-binding-rework cycle is the right pacing.  Marked BLOCKED with full audit data.  test_lifetime_safety remains xfailed (§J.16) → strict-fails when this lands → flags unmark moment.
- **CYCLE-159.1-BLOCKED-CONVERGENCE-DETECTION** ⛔ (filed by CYCLE-185 iter-3 FAIL per Rule 5): sparse_eigensolver LOBPCG converges correctly on diagonal/clustered spectra (Test 5 PASS at cos-sim=1.000 across iters 1-3) but doesn't converge on tridiagonal spectra (Tests 1/4 hit max_iter without progress). 3 iter attempts: iter-1 fixed eigenvalue ordering bug (1/5 PASS), iter-2 negate-M trick (2/5), iter-3 relative-residual (still 2/5). Likely stagnation rather than non-convergence — needs instrumented stagnation diagnosis or Phase B re-do on LOBPCG convergence behavior for tridiagonal matrices. Diffmap+dpt rewrites (CYCLE-183/184/185 in original 4-cycle plan) are blocked behind this. Net artifacts produced: design doc ~210 LOC, LOBPCG impl ~625 LOC, ctest ~731 LOC, 2/5 tests PASS, bug well-characterized.
- **CYCLE-227-LAND-§J.17-AUDIT-CONSUMERS-BEFORE-API-CHANGE** (pure-Opus, ~70 LOC into state/style-rules.md): captures CYCLE-225's regression. Before changing a shared C++/Python interface (binding return type, struct field rename), grep ALL consumers in `python/` and `python/src/` and pick atomic-update / dual-typed / versioned-coexistence based on diff size. Generalizes §J.13's deeper-call-site-recurrence from FIX direction into CHANGE direction. §J series now 17 sub-rules. §J.5 risk-pacing recovery: HIGH FAIL → LOW Opus rule-distillation.

## 🎯 STRATEGIC SCOPE (2026-04-29 round 2 — locked)

Frobenius NMF only (KL / IS / NB-GLM / β-divergence dropped); fast PCA / SVD with regularization (L1, L2, NN, orthogonality); cross-validation across the board (speckled-mask for NMF, held-out fold for PCA). Then port aggressively from Bioconductor (scran, scater, DropletUtils, BiocSingular), Seurat, scanpy, fgsea, AUCell, Harmony, scvi-tools.

**Bare-metal CUDA only** (cuBLAS / cuSPARSE / cuSOLVER / cuRAND / cuDNN / CUTLASS). PyTorch / LibTorch / JAX / TensorFlow / mxnet at runtime are forbidden.

## 🟡 Queued (priority-ordered)

-1. **CYCLE-159.1-EXPANDED — DIFFMAP + DPT SPARSE-EIGENSOLVER + DPT API REFACTOR** (filed by CYCLE-159 + CYCLE-160 + CYCLE-161): embed/diffmap is 14× SLOWER + CRASH at 30k; embed/dpt is **541× SLOWER** at 10k (CYCLE-161 confirmed §J.6 + found bonus API design bug — dpt re-runs full eigendecomp every call). Combined fix: (a) replace dense `cusolverDnSsyevd` with sparse eigensolver (LOBPCG/Lanczos) for both kernels, (b) refactor dpt API to mirror scanpy's `compute_diffusion_eigenvectors()` once + `dpt(diffusion_result, root_cell)` many times. Phase B research required first. ~2-3 days.
0. **CYCLE-148.1-SCRUBLET-DEEPER-DIAG** (filed by CYCLE-153 iter-1 FAIL): kNN finds too many synthetic neighbors per real cell — either (a) synthetic doublets too close to real cells in PCA space (generation bias), (b) k too small (estimator bias), or (c) self-loops inflating real-cell counts. Needs Phase B Haiku research dispatch first (lit-scout: Wolock 2019 + scrublet Python source neighbor-counting; code-reader: scrublet repo). Then iter-2. Two-iteration rule (Rule 5) means failure here would mark `blocked`.
1. **CYCLE-122-ENRICHMENT-ZERO-OUTPUT-DIAG** (filed by CYCLE-119): 6 cycle-13/44 tests fail with effectively-zero outputs (AUCell mean_high=0, fgsea q-values all 0, ssgsea/progeny variance=0, 2 determinism subnormal-float noise). NOT a CYCLE-105 regression — these tests were "Correctness: pending" in cycle 13 and never debugged. Likely shared root cause (uninitialized scratch buffer suspected). 1-3 hour kernel debug dispatch.
2. **CYCLE-123-SCANPY-PARITY-AUDIT**: side-by-side `scanpy.pp.normalize_total + log1p + highly_variable_genes + scale + neighbors + leiden + umap + rank_genes_groups` against shipped wrappers on real `.1pz`. Drives `docs/notebooks/scanpy_parity.ipynb`. NO infra blockers.
3. **CYCLE-118.5-PEARSON-HVG-FLAVOR-WIRING** (small): wire `pearson_residual_variance` into `preprocess/hvg.h` as flavor='pearson_residuals', matching `scanpy.experimental.pp.highly_variable_genes(flavor='pearson_residuals')`. Adds Python wrapper exposure + docs page. ~80 LOC.
4. **CYCLE-125-HARMONY-NATIVE**: native Harmony from scratch (existing `integrate/harmony.h` was factornet-era). Substantial — split into design pass first.
5. **CYCLE-116-SCRAN-NORMALIZATION-PORT** ⛔: port `scran::computeSumFactors` — verify bit-for-bit vs scran R. Blocked on R install.
6. **CYCLE-117-BIOCSINGULAR-IRLBA-AUDIT**: fresh IRLBA only if benchmarks show regime where it wins vs deflation. Speculative.
7. **CYCLE-113.1-STREAMING-ASSOCIATIVITY** (real algorithmic bug): per-shard reduction non-associative; `HvgDeviance5_StreamingAssociativity` Jaccard 0.998, Spearman 0.024 between single-pass and streaming.
8. **CYCLE-99-FOLLOWUP-SCALE-BENCH**: backfill `state/pareto-frontier.md` row for `preprocess/scale`.
9. **CYCLE-107-WRAPPER-AUDIT-FOLLOWUPS**: CYCLE-19-FOLLOWUP-SCANPY-SIG-PARITY + CYCLE-24-FOLLOWUP-RCPPML-PATTERN.

## ⛔ Blocked

(see `state/blockers.md` — INFRA-CUVS-CUGRAPH-INSTALL gates 8 tier-2, 9, 10; INFRA-R-SCRY-INSTALL gates 3 deviance HVG tests + CYCLE-116 scran verify)

## Conventions

- 🔴 = active in current cycle
- 🟡 = queued for an upcoming cycle, in priority order
- ⛔ = blocked on something in `state/blockers.md`
- ✅ = done — moves to `state/cycle-log.md` and is removed from this file
- One line each. If you need more context, link to a design doc or follow-up entry.
