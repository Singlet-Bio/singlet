# singlet-gpu SOTA baseline dependencies
# Install into the benchmark venv:
#   python -m venv bench_env && source bench_env/bin/activate
#   pip install -r bench/refs/requirements.txt
#
# GPU note: cupy-cuda12x / rapids require CUDA 12.x on the compute node.
# CPU-only fallback: comment out the rapids/cupy group — scanpy baselines still run.

# ── Core single-cell / IO ─────────────────────────────────────────────────
scanpy>=1.10.0
anndata>=0.10.0
numpy>=1.26.0
scipy>=1.12.0
scikit-learn>=1.4.0
pandas>=2.0.0

# ── RAPIDS GPU stack (CUDA 12.x) — primary GPU baselines ─────────────────
# Provides: rapids-singlecell (GPU scanpy), cuml (PCA/UMAP/kNN), cugraph (Leiden)
rapids-singlecell>=0.10.0
cupy-cuda12x>=13.0.0
cuml-cu12>=24.4.0
cugraph-cu12>=24.4.0

# ── FAISS GPU — kNN baseline ──────────────────────────────────────────────
faiss-gpu>=1.7.4

# ── RNA velocity ─────────────────────────────────────────────────────────
scvelo>=0.3.2
cellrank>=2.0.0

# ── Probabilistic models + integration ───────────────────────────────────
scvi-tools>=1.1.0

# ── Disease scoring ───────────────────────────────────────────────────────
scdrs>=1.0.3

# ── Pathway enrichment (Python side) ─────────────────────────────────────
# fgsea is invoked via Rscript subprocess — no Python package needed here.
# decoupler provides AUCell-equivalent and pathway scoring in Python.
decoupler-py>=1.6.0

# ── UMAP baseline ────────────────────────────────────────────────────────
umap-learn>=0.5.6

# ── Misc / timing ────────────────────────────────────────────────────────
psutil>=5.9.0        # RSS memory for CPU baselines
tqdm>=4.66.0
